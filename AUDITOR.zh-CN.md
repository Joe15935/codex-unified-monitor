# Pro Tier Auditor / 套餐额度审计

[English](AUDITOR.md) · **简体中文** · [返回使用说明](README.zh-CN.md)

本功能通过长期受控观察，比较实际容量与独立 Pro 5x 基准的相似程度。它可以提供可核对的 Token、官方额度变化和计算过程，**不能单独证明 OpenAI 后台 entitlement 配置错误**。没有内置的固定 Pro 5x 美元基准，也不会自动发送投诉或报告。

## 怎样开始

1. 打开“套餐审计”。被动历史记录展示已观察到的周额度变化、期间的 Token 和排除原因。
2. 选择预期套餐、完整模型名、推理强度、每次响应的输入长度范围与任务类型。确认关闭 Fast、避免子代理，并仅使用本机已登录订阅的任务，不混入 API Key、其他设备、云端任务或其他共享额度功能。
3. 在任务开始前点击“开始受控观察”，改变条件前停止。软件记录这些声明，不会修改 Codex 设置，也不会把用户声明当作日志独立验证。价格在开始时冻结，不能事后把旧活动声明为受控样本。
4. 使用独立 Pro 5x 账号完成可比的观察，导出其 JSON，再从“独立的 Pro 5x 基准”导入。来源可填写公开 GitHub / 官方链接，或用 `private-reference` 表示私下提供；该字段只作引用，不会自动访问链接。软件不会验证对方实际购买的套餐。
5. 点击“导出证据报告”，一次生成 JSON、HTML、CSV。HTML 跟随当前语言；JSON、CSV 保留原始字段与标识。文件先留在本机，由你决定是否提交 Support。

当前版本显示当前账号最近一次观察，旧记录继续保存在数据库中。开始下一次观察前，建议导出已完成的报告，以便直接查阅。

## 每个区间如何对账

软件将成功的官方读取按时间排序，从上一次变化边界一直汇总到下一次变化，纳入中间额度未变化的读取。Token 使用 `(开始时间, 结束时间]` 内去重后的事件，精度为秒。“任务样本”按不同的会话 / 轮次计数，不等于响应数或轮询次数。尚未变化的尾段单独保留。

表格显示剩余额度；正的 Δ 表示已用额度增加的百分点。轮询只能看到读取时的变化，无法捕获每一笔服务端扣减。

```text
API 等价值 = (未缓存输入 × 输入价 + 缓存输入 × 缓存价 + 输出 × 输出价) / 1,000,000
每 1% 对应的等价值 = 区间 API 等价值 / 区间消耗百分点
实测周容量 = 100 × 符合条件区间的 API 等价值之和 / 消耗百分点之和
```

价格使用观察开始时冻结的公开基础价，不随总览页的估值选项变化。服务档位、长上下文、工具费用与历史价格调整未被还原。这是观察到的对应关系，不是 OpenAI 的计费公式。服务端延迟扣量和本地日志延迟写入可能影响边界归属，10 分钟等待期只能减少这类不确定性。

## 什么样的证据才参与判断

缺少实时周额度、重置、额度回退、重置次数变化、切换账号、套餐变化、读取间断、额度耗尽、缺少本机 Token、未知模型价格、任务身份缺失、违反测量条件或仍处等待期的区间会保留，但排除出推算。观察期间出现新的解析错误，也会阻止区间参与判断。

模型或推理强度混合的区间整体排除，不会只保留某个模型的 Token，却把整个账号的额度消耗归给它。输入范围按每次响应的原始输入 Token 计算，它只是可观察的上下文代理指标，不是配置中的最大上下文长度。

| 置信程度 | 最低条件 |
|---|---|
| 不足 `INSUFFICIENT` | 下列条件中有任一项不满足 |
| 中等 `MEDIUM` | 12 个额度变化区间、20 个不同任务、8 小时符合条件的覆盖、消耗 20 个百分点、Token 全部可定价、费率变异系数不超过 25%，且敏感性范围有限 |
| 高 `HIGH` | 满足中等条件，并达到 30 个区间、30 个任务、24 小时、30 个百分点；每个参与区间的 Fast / 子代理条件均有日志证据 |

置信程度是证据质量的启发式分级，不是经校准的概率。Fast 或子代理控制只有用户声明时，最高为“中等”；缺失字段始终保留“未知”，不会推定 Fast 已关闭。

`±` 表示按额度加权的区间费率标准差，不是统计上的 95% 置信区间。敏感性范围同时考虑均值 ±2 倍加权标准差，以及端点读数按 ±1 个百分点处理的量化误差。连续合格端点的误差可抵消，被排除的间隔则扩大不确定性；这也不是官方精度保证。

## 与基准怎样比较

基准需要是 30 天内完成的独立 Pro 5x 观察，采用同一方法。导入会检查校验和、价格指纹、区间不重叠、计数和计算一致性，并重新计算证据质量。同一来源的报告不能作为自己的独立基准。校验和不能认证套餐身份或测量真伪。

双方必须匹配模型、推理强度、输入范围、任务类型和价格指纹；平均输入差异不超过 25%，缓存命中率差异不超过 10 个百分点，输出 / 输入差异不超过 5 个百分点。每次响应的工具调用差异上限为 0.25 或基准的 25%，取较大值。这些条件依然不能证明任务完全相同。

只有相对容量的整个敏感性范围都落入下列区间，才给出相似性判断：

| 结果 | 相对基准范围 |
|---|---|
| `PRO-5X-LIKE` | 0.65–1.50 倍 |
| `PRO-20X-LIKE` | 2.80–5.60 倍 |
| `INCONCLUSIVE` | 其他范围，或任一前提不足 |

20 / 5 = 4 倍是明确的比较假设，不是每周固定美元容量的承诺。缺少基准、当前额度过期或双方不具可比性时，即使可以展示描述性的容量估算，分类仍为 `INCONCLUSIVE`。

## 隐私与证据边界

报告包含 Token、模型、推理强度、官方读数时间戳、冻结价格、排除原因和计算方法。省略邮箱、账号标识、原始会话 / 响应 ID、路径、提示词和工具内容。报告来源使用本地随机化名，任务使用报告内序号。导出文件仅当前用户可读写，不覆盖旧文件；基准导入上限为 2 MiB。

官方说明和 issue 研究的原始链接、完整算法与真实验证结果见 [英文方法说明](AUDITOR.md) 和 [验证记录](TEST_REPORT.md)。用户报告可以提供调查线索，不能直接作为已验证的后台权益错误证据。
